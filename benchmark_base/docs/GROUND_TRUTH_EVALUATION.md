# 真值评测

真值必须在 manifest 中说明路径或话题、消息类型、坐标系、时间基准和外参。标准化后先求所有成功算法与真值的共同有效时间区间，再以 SE(3) 插值同步。

刚性定位系统默认采用 SE(3) 对齐；平面任务可显式选 yaw+translation。Sim3 只允许尺度不确定系统，并必须在报告中标出。支持的定量指标包括 ATE、RPE、平移/旋转误差和基于真值切向方向定义的横纵向误差。

当前 bag 无独立真值。系统历史 odometry/TF 不作为真值，因此不得生成 ATE/RPE 或“绝对精度”排名。
